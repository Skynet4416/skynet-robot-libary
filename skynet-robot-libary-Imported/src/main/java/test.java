import com.github.iprodigy.physics.util.vector.Vector;

public class test {
    // Vector velocity = new Vector(40.2,0.0,0.0);
    // Vector rotational_velocity = new Vector(0.0,0.0,)
    // velocity_direction.crossProduct3D(axis_of_rotation).multiply(0.5).multiply(lift_coeficent)
    //             .multiply(Constants.density_of_air).multiply(cross_section_area_of_ball)
    //             .multiply(Math.pow(state.velocity.getMagnitude(), 2)));
}
